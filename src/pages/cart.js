import React from 'react'
import styled from "styled-components"

import Layout from '../components/layout'

const Cart = () => {
  return (
    <Layout>
      <Wrapper>
        <p>Cart page currently in construction!</p>
      </Wrapper>
    </Layout>
  )
}

export default Cart

const Wrapper = styled.div`
  margin: 40px;
`